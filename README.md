# MASBot Analysis App

Streamlit app for uploading circle and dot trajectory files and generating plots and playback.

## Run locally

```bash
pip install streamlit numpy matplotlib scipy
streamlit run app.py
```

## Usage

Upload `.mat` files containing `Xmat` and `Ymat` to visualize and analyze trajectories.
